'use strict';

/**
 * param-application controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::param-application.param-application');
